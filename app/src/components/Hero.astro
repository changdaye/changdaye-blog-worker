<section class="section card" style="padding: 32px; overflow: hidden; position: relative;">
  <div style="position: absolute; inset: 0; background: radial-gradient(circle at top right, rgba(106,166,255,0.22), transparent 35%), radial-gradient(circle at left center, rgba(143,188,255,0.12), transparent 30%);"></div>
  <div style="position: relative; display: grid; gap: 16px; max-width: 720px;">
    <span class="tag">Rewrite First</span>
    <h1 style="margin: 0; font-size: clamp(36px, 6vw, 62px); line-height: 1.05;">A fresh Astro blog rebuilt for Cloudflare Workers.</h1>
    <p class="muted" style="margin: 0; font-size: 18px; line-height: 1.7;">新的博客会先完成全新的页面与内容架构，再有选择地引入旧文章和图片，而不是把旧 Jekyll 结构搬过来。</p>
  </div>
</section>
